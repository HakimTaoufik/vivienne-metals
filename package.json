{
  "name": "vivienne-metals",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/*.test.mjs && python -m unittest discover -s tests -v",
    "collect": "python -m collector.cli collect",
    "signals": "node scripts/signals.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "jsdom": "30.1.0"
  }
}
