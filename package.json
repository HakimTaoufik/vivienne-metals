{
  "name": "vivienne-metals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs && python -m unittest discover -s tests -v",
    "collect": "python -m collector.cli collect",
    "signals": "node scripts/signals.mjs"
  }
}
